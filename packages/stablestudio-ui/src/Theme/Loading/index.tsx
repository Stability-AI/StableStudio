import { Rainbow } from "./Rainbow";
import { Spinner } from "./Spinner";

export declare namespace Loading {
  export { Spinner, Rainbow };
}

export namespace Loading {
  Loading.Spinner = Spinner;
  Loading.Rainbow = Rainbow;
}
